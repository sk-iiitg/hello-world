name: Deploy
on:
  workflow_dispatch: {}
  push:
    branches-ignore:
      - master # Deploys happen before merge with bors.
jobs:
  run-sanity-tests:
    name: Run Sanity Tests
    uses:  ./.github/workflows/testing.yaml
  
